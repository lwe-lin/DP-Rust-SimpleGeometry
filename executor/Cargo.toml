
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
dp_rust_simplegeometry = { path = "../game", optional = true }

[features]
default = ["dp_rust_simplegeometry"]
dylib = ["fyrox/dylib"]
