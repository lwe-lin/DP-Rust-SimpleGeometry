
[package]
name = "game_dylib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
dp_rust_simplegeometry = { path = "../game", default-features = false }

[features]
default = ["dp_rust_simplegeometry/default"]
dylib-engine = ["dp_rust_simplegeometry/dylib-engine"]
